<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white_color"/> <!-- Background -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black"/> <!-- Border -->
    <corners android:radius="18dp"/> <!-- Rounded corners (optional) -->
</shape>
