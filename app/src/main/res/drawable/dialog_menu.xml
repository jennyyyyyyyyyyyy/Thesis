<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white_color"/> <!-- Background -->
    <stroke
        android:width="1dp"
        android:color="@color/black_color"/> <!-- Border -->
    <corners
        android:bottomRightRadius="10dp"
        android:topRightRadius="10dp"/> <!-- Rounded corners (optional) -->
</shape>
