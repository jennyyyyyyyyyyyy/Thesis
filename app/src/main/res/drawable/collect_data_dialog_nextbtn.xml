<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white_color"/>
    <stroke
        android:width="1dp"
        android:color="@color/grey_color"/>
    <corners android:radius="3dp"/> <!-- Rounded corners (optional) -->
</shape>
