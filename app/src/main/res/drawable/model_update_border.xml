<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>
    <stroke
        android:width="1dp"
        android:color="@color/black_color"/><!-- Background -->
    <corners android:radius="5dp"/> <!-- Rounded corners (optional) -->
</shape>
